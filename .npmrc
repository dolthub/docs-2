; @dolthub/react-components and the React 19 toolchain have peer-dependency
; ranges that don't all satisfy npm's strict peer resolver. Keep the same
; behavior the per-site `npm install --legacy-peer-deps` had before the
; workspace migration, now applied to the single root install.
legacy-peer-deps=true
