---
title: "Database (legacy)"
description: Legacy index — endpoints have moved to per-resource pages. Old deep links still anchor to the right place.
---

# Database (legacy)

_API version: v1alpha1_

> **Reorganised.** These endpoints have moved to separate per-resource pages.
> Each section heading below is preserved so existing deep links
> (`/products/dolthub/api/database#…`) still anchor to the right place — and
> each one points to the new home.

## Create database

→ Moved to [Databases / Create database](v1alpha1/databases#create-database).

## Fork database

→ Moved to [Databases / Fork database](v1alpha1/databases#fork-database).

## List forks

→ Moved to [Databases / List forks](v1alpha1/databases#list-forks).

## Create pull request

→ Moved to [Pull Requests / Create pull request](v1alpha1/pull-requests#create-pull-request).

## Get pull request details

→ Moved to [Pull Requests / Get pull request details](v1alpha1/pull-requests#get-pull-request-details).

## Update a pull request

→ Moved to [Pull Requests / Update a pull request](v1alpha1/pull-requests#update-a-pull-request).

## List pull requests

→ Moved to [Pull Requests / List pull requests](v1alpha1/pull-requests#list-pull-requests).

## Create a pull request comment&#x20;

→ Moved to [Pull Requests / Create a pull request comment](v1alpha1/pull-requests#create-a-pull-request-comment).

## Merge pull request

→ Moved to [Pull Requests / Merge pull request](v1alpha1/pull-requests#merge-pull-request).

## Upload a file

→ Moved to [File Uploads / Upload a file](v1alpha1/uploads#upload-a-file).

## Create a branch

→ Moved to [Branches / Create a branch](v1alpha1/branches#create-a-branch).

## List branches

→ Moved to [Branches / List branches](v1alpha1/branches#list-branches).

## Create a tag

→ Moved to [Tags / Create a tag](v1alpha1/tags#create-a-tag).

## List tags

→ Moved to [Tags / List tags](v1alpha1/tags#list-tags).

## Create a release

→ Moved to [Releases / Create a release](v1alpha1/releases#create-a-release).

## List releases

→ Moved to [Releases / List releases](v1alpha1/releases#list-releases).

## List operations

→ Moved to [Jobs / List operations](v1alpha1/jobs#list-operations).

## List jobs

→ Moved to [Jobs / List jobs](v1alpha1/jobs#list-jobs).
