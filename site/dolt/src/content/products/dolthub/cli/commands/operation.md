---
title: "dh operation"
description: "Work with asynchronous operations"
---

<!-- Generated by dh generate-docs (renderer 1). Do not edit.
Provenance: {"cli_version":"dev","source_commit":"08416574e31200b7bf626bf12d6992bd2dacce18","source_dirty":false,"publication_ready":false}
Regenerate with: dh generate-docs --output DIRECTORY
-->

## dh operation {#dh-operation}

Work with asynchronous operations

### Usage

```text
dh operation [command] [flags]
```

### Flags

| Flag | Short | Type | Default | Description |
| --- | --- | --- | --- | --- |
| <code>--help</code> | <code>-h</code> | bool | <code>false</code> | help for operation |

### Related commands

- [dh](/products/dolthub/cli/commands#dh)
- [dh operation list](/products/dolthub/cli/commands/operation#dh-operation-list)
- [dh operation view](/products/dolthub/cli/commands/operation#dh-operation-view)
- [dh operation watch](/products/dolthub/cli/commands/operation#dh-operation-watch)

## dh operation list {#dh-operation-list}

List database operations

Select a database with --db, then DH_REPO, saved repo configuration, or local Dolt remotes (in that order). Use dh config set repo OWNER/DB to save a default. Multiple remotes prompt interactively; scripts must select a database explicitly when discovery is ambiguous.

Use --json with a comma-separated list of fields for structured output. --jq and --template transform the selected JSON and require --json.

### Usage

```text
dh operation list [flags]
```

### Flags

| Flag | Short | Type | Default | Description |
| --- | --- | --- | --- | --- |
| <code>--db</code> | <code>-R</code> | string | <code>""</code> | Select a database using &#91;HOST/&#93;OWNER/DB |
| <code>--help</code> | <code>-h</code> | bool | <code>false</code> | help for list |
| <code>--jq</code> |  | string | <code>""</code> | Filter JSON output using a jq &#96;expression&#96; |
| <code>--json</code> |  | stringSlice | <code>&#91;&#93;</code> | Output JSON with the specified &#96;fields&#96; |
| <code>--limit</code> |  | int | <code>30</code> | Maximum number of operations |
| <code>--template</code> |  | string | <code>""</code> | Format JSON output using a Go template |

### Constraints

- --limit must be positive.

### JSON fields

<code>cancelable</code>, <code>created&#95;at</code>, <code>error</code>, <code>id</code>, <code>result</code>, <code>status</code>, <code>type</code>

### Output

Prints operations for the selected database, or a JSON array of selected fields, fetching pages up to --limit.

### Examples

```bash
dh operation list --db OWNER/people
```

### Related commands

- [dh operation](/products/dolthub/cli/commands/operation#dh-operation)

## dh operation view {#dh-operation-view}

View an asynchronous operation

Use --json with a comma-separated list of fields for structured output. --jq and --template transform the selected JSON and require --json.

### Usage

```text
dh operation view ID [flags]
```

### Arguments

| Argument | Optional | Repeated | Description |
| --- | --- | --- | --- |
| <code>ID</code> | false | false | Operation ID returned by an asynchronous command; resolved on the configured host. |

### Flags

| Flag | Short | Type | Default | Description |
| --- | --- | --- | --- | --- |
| <code>--help</code> | <code>-h</code> | bool | <code>false</code> | help for view |
| <code>--jq</code> |  | string | <code>""</code> | Filter JSON output using a jq &#96;expression&#96; |
| <code>--json</code> |  | stringSlice | <code>&#91;&#93;</code> | Output JSON with the specified &#96;fields&#96; |
| <code>--template</code> |  | string | <code>""</code> | Format JSON output using a Go template |

### Constraints

- Authentication is required. Uses DH_HOST or saved/default host configuration, not a database selector.

### JSON fields

<code>cancelable</code>, <code>created&#95;at</code>, <code>error</code>, <code>id</code>, <code>result</code>, <code>status</code>, <code>type</code>

### Output

Prints current operation details or selected JSON fields. This is a snapshot; use dh operation watch to wait for completion.

### Examples

```bash
dh operation view OPERATION_ID
```

### Related commands

- [dh operation](/products/dolthub/cli/commands/operation#dh-operation)

## dh operation watch {#dh-operation-watch}

Watch an asynchronous operation

Use --json with a comma-separated list of fields for structured output. --jq and --template transform the selected JSON and require --json.

### Usage

```text
dh operation watch ID [flags]
```

### Arguments

| Argument | Optional | Repeated | Description |
| --- | --- | --- | --- |
| <code>ID</code> | false | false | Operation ID returned by an asynchronous command; resolved on the configured host. |

### Flags

| Flag | Short | Type | Default | Description |
| --- | --- | --- | --- | --- |
| <code>--help</code> | <code>-h</code> | bool | <code>false</code> | help for watch |
| <code>--interval</code> |  | duration | <code>1s</code> | Initial polling interval |
| <code>--jq</code> |  | string | <code>""</code> | Filter JSON output using a jq &#96;expression&#96; |
| <code>--json</code> |  | stringSlice | <code>&#91;&#93;</code> | Output JSON with the specified &#96;fields&#96; |
| <code>--template</code> |  | string | <code>""</code> | Format JSON output using a Go template |

### Constraints

- Authentication is required. --interval must be positive; it sets the initial polling interval. Uses the configured host.

### JSON fields

<code>cancelable</code>, <code>created&#95;at</code>, <code>error</code>, <code>id</code>, <code>result</code>, <code>status</code>, <code>type</code>

### Output

Polls until the operation finishes and prints its details or selected JSON fields. Progress goes to stderr; failed or canceled operations return a nonzero exit status. Stopping the local wait does not cancel remote work.

### Examples

```bash
dh operation watch OPERATION_ID --json id,status,result
```

### Related commands

- [dh operation](/products/dolthub/cli/commands/operation#dh-operation)
